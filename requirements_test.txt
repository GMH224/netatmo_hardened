# Tier 1 (pure logic) - runs on any Python >= 3.11, no Home Assistant needed.
pytest==8.3.4

# Tier 2 (integration) - requires Python >= 3.14.2 and Home Assistant 2026.9.
# Pinned deliberately: this fork's behaviour is defined against these exact
# versions. See docs/COMPATIBILITY.md before changing either pin.
homeassistant==2026.9.3
pytest-homeassistant-custom-component==0.13.*
pytest-asyncio==0.24.0
pyatmo==9.9.0

# Static analysis
ruff==0.15.11
